services:
  - type: web
    name: askyourdoc-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
